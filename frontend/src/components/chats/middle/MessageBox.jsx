import { Box } from '@mui/material';

const MessageBox = () => {
  return (
    <Box
      sx={{
        backgroundColor: 'red',
        width: '300px',
        borderRadius: '5px',
        p: 1,
      }}
    >
      MessageBox
    </Box>
  );
};

export default MessageBox;
